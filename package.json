{
  "name": "portfolio-content-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Content pipeline for Jimena Sanchez Curto's portfolio",
  "scripts": {
    "build": "node build/build.js",
    "dev": "node build/server.js",
    "validate": "node build/build.js --validate-only"
  }
}
